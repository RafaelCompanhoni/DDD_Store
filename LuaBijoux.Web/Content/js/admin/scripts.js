/* Main JS */

$(document).ready(function () {
    // Configurações do plugin 'datepicker bootstrap'
    $('.datepicker').datepicker({
        format: 'dd/mm/yyyy',
        autoclose: true
    });
});
